name: smart_waste_management
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.1.2
  shared_preferences: ^2.2.2
  http: ^1.4.0
  flutter_map: ^6.1.0
  geolocator: ^11.0.0
  latlong2: ^0.9.1
  geocoding: ^2.1.1
  flutter_map_cancellable_tile_provider: ^2.0.0
  url_launcher: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.13

flutter:
  uses-material-design: true
